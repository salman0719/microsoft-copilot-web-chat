{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "dev-old": "vite",
    "dev": "concurrently \"vite --config vite-embed-child.config.ts\" \"vite --config iframe-container/vite.config.ts\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-old": "tsc && vite build",
    "build-child": "vite build --config vite-embed-child.config.ts",
    "build-parent": "tsc && vite build --config iframe-container/vite.config.ts",
    "build": "concurrently \"npm run build-child\" \"npm run build-parent\"",
    "preview": "npm start",
    "start-old": "tsc && vite build && vite preview",
    "start": "concurrently \"vite preview --config vite-embed-child.config.ts\" \"vite preview --config iframe-container/vite.config.ts\""
  },
  "author": "Salman Hossain",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.0.0",
    "node-fetch": "^2.5.0",
    "restify": "^11.1.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "node-dev": "^8.0.0",
    "sass-embedded": "^1.77.8",
    "typescript": "^5.5.3",
    "vite": "^5.4.1"
  }
}